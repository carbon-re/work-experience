# Mac setup. From inside the repo, run:
#   brew bundle install
#
# git is not listed here - you need it before you can clone the repo.
# See Step 1 in the README.

brew "pyenv"
brew "pantsbuild/tap", trusted: true
brew "node"
