# The sample plant CSVs, exposed to pants so that Python targets (e.g. the
# example dashboard) can depend on them and have them copied into the sandbox.
resources(
    name="csvs",
    sources=["*.csv"],
)
